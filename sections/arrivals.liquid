<section class="arrivals" id ="arrivals">
    <div class = "Nav_Menu">
       <div class="PF"> {{ "PLANT FAMILIES" }}</div>
       <div class="GF"> {{ "GIFTS" }}</div>
       <div class="FL"> {{ "FLOWERS" }}</div>
       <div class="FP"> {{ "FEATURED PRODUCTS" }}</div>
    </div>
    <div class = "new_arrivals">
      {% for product in collections.NewArrivals.products %}
       <div class = "container">
        <img src = "{{ product.featured_image | img_url: 'medium' }}" alt = "New Arrivals">
       <div class = "title">
          {{ product.title }}</div>
      <div class = "price">
          {{ product.price }}</div>
      </div>
      {% endfor %}
</div>
</section>
 

<style>
    #arrivals .new_arrivals{
        display: flex;
        flex-wrap: wrap;
        gap: 1rem;
        justify-content: center;
        align-items: center;
        margin-top: 5rem;
    }
    #arrivals .container{
        display: flex;
        flex-wrap: wrap;
        flex-direction: column;
        padding: 1px;
    }
    #arrivals .Nav_Menu{
        display: flex;
        justify-content: center;
        align-items: center;
    }
</style>

{% schema %}
{
    "name": "NewArrivals",
    "settings": [],
    "presets": [
       {
        "name": "NewArrivals"
    }
    ]
}
{% endschema %}